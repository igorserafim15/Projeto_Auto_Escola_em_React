import React from 'react';
import FilledButton from './FilledButton';

export default {
  title: 'Buttons',
  component: FilledButton
}

export const primary = () => <FilledButton>Clique aqui</FilledButton>
